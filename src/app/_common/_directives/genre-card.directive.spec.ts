import { GenreCardDirective } from './genre-card.directive';

describe('GenreCardDirective', () => {
  it('should create an instance', () => {
    const directive = new GenreCardDirective();
    expect(directive).toBeTruthy();
  });
});
