import { PodcastsLinksInFavoriteDirective } from './podcasts-links-in-favorite.directive';

describe('PodcastsLinksInFavoriteDirective', () => {
  it('should create an instance', () => {
    const directive = new PodcastsLinksInFavoriteDirective();
    expect(directive).toBeTruthy();
  });
});
